name: My cool Workflow

on:
  push:  
  pull_request:
  workflow_dispatch:

jobs:
    myfirstjob: 
              name: First Job
              runs-on: ubuntu-latest
              steps:
              - name: Step One
                uses: actions/checkout@v3
              - name: Step Two
                run: |
                  touch {{secrets.MY_SECRET}}.yaml
                  echo "Please Alex is friday let me scape" >> {{secrets.MY_SECRET}}.yaml
                  cat {{secrets.MY_SECRET}}.yaml
                  pwd
    mySecondJob:
                needs: [myfirstjob]
                name: Second Job
                runs-on: macos-latest
                steps: 
                - name: Step One
                  run: date
    myThirdJob:
              name: Third Job
              runs-on: windows-latest
              steps: 
                - name: Step One
                  run: date
          
