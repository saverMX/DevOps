name: My cool Workflow

on:
  push:  
  pull_request:
  workflow_dispatch:

jobs:
    myfirstjob: 
              name: First Job
              runs-on: ubuntu-latest
              steps:
              - name: Step One
                uses: actions/checkout@v3
              - name: Step Two
                run: |
                  touch Touched.yaml
                  echo "Please Alex is friday let me scape" >> Touched.yaml
                  cat Touched.yaml
                  pwd
